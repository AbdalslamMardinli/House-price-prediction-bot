# House-price-prediction-bot
This project predicet the houses price in londen it's predtict accurasy is 89,9 and it can reach more if i had an alternative dataset
If it's your first time lunching a python code you have to install the libraries by doing the next steps
1. if you're using visual studio code press Ctrl+ Shift + P. or Use the menu bar: Go to Terminal in the top menu and select New Terminal.
2. Write in the terminal "pip install numpy"
3. after installing numpy install pandas by doing tha same thing and writing this "pip install pandas" 
